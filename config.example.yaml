# Engagement Brain — Global Config
# Generated by /onboard. Committed to the repo — shared by all contributors.
#
# Rules:
#   - Keep values in double quotes
#   - No colons (:) inside values
#
# SOW-specific config (Drive folder, Slack channels, SOW doc URL,
# engagement type and context) lives in sows/<sow>/sow.config.yaml

# Full legal or formal client name (e.g. "ZenQMS Inc.", "ACME Corp.")
CLIENT_NAME: "Your Client Inc."

# Full name of the engagement brain owner — the person who forked and set this up
OWNER_NAME: "Your Name"

# Their role on this engagement (e.g. "TPM", "ML Lead", "Tech Lead")
OWNER_ROLE: "TPM"
